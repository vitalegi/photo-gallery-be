spring:
  datasource:
    url: jdbc:h2:mem:db1
    username: sa
    password: ''
  liquibase:
    enabled: true
    change-log: 'classpath:db/changelog/db.changelog-master.yaml'
  jpa:
    hibernate:
      ddl-auto: validate


metrics:
  skipIfLessThan: -1
